# Repository instructions

- Match the surrounding style; never compact multiple statements, JSX elements, or CSS declarations onto one line.
- Format every touched file, then run `npm run compile`; run browser builds for behavior changes.
